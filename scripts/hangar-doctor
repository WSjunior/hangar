#!/usr/bin/env bash
# hangar-doctor [--qr]: diagnóstico da instalação, uma linha por item, com conserto.
set -euo pipefail
cd "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")/../backend"
exec uv run --quiet python -m app.doctor "$@"
